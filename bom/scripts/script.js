const list = document.querySelector('ul');
const input = document.querySelector('input');
const button = document.querySelector('button');

button.addEventListener('click', () => {
  console.log(input.value)

  // const addChapter = input.value;
  //If the input value is not empty then do:
  if(input.value != []){
    //Create a li element

    //Create a button

    //Update the list item to have the input value

    //Update the button to have an X

    //Append the button to the li

    //Append the li to the list

    //Add an event listener to the delete btn with deletes the li from the list 

  }
    



// const listcontainer = document.createElement('li');
// // const listText = document.createElement('span');
// const listBtn = document.createElement('button');

// // listcontainer.appendChild(listText);
// listText.textContent = addChapter;
// listcontainer.appendChild(listBtn);
// listBtn.textContent = 'X';
// list.appendChild(listcontainer);

// listBtn.addEventListener('click', () => {
//   list.removeChild(listcontainer);
// });

// input.focus();

});
    